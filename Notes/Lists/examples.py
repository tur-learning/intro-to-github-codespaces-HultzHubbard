# list example
numbers = [1, 2, 3, 4, 5, 6, 70, -9, 300]

# cat list (which value is in [#] spot)
numbers[0]
numbers[3]

# duplicating lists
numbers += numbers

#cat lists (can go backwards - right to left)
numbers[-1]

# cutting lists
start = 2
end = 4
numbers[start:end]